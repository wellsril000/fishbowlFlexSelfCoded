# Railway configuration
# Note: Railway needs to detect Python from backend/requirements.txt
# Make sure to set Root Directory to "backend" in Railway service settings

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

